namespace Core.Domain.Enums
{
    public enum ProductTypeEnum
    {
        Product = 0,  // Regular product that all users can tag
        Own = 1       // User's own tag that only they can use
    }
}
